/**
 * Export FileManagerContext, FileManagerProvider, and useFileManager
 */
import FileManagerContext, { FileManagerProvider } from './FileManagerContext';
import { useFileManager } from './useFileManager';

export { FileManagerProvider, useFileManager };
export default FileManagerContext;
